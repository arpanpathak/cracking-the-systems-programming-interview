---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: gpuworkloads.gpucloud.example.com
spec:
  group: gpucloud.example.com
  names:
    kind: GpuWorkload
    listKind: GpuWorkloadList
    plural: gpuworkloads
    shortNames:
    - gw
    singular: gpuworkload
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.gpuCount
      name: GPUs
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: GpuWorkload is the Schema for a GPU cloud workload.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GpuWorkloadSpec defines the desired state of a GPU-accelerated
              cloud workload.
            properties:
              args:
                description: Args appends container arguments.
                items:
                  type: string
                type: array
              command:
                description: Command overrides the container command.
                items:
                  type: string
                type: array
              containerPort:
                default: 8000
                description: ContainerPort is the port exposed by the container. Defaults
                  to 8000.
                format: int32
                minimum: 1
                type: integer
              gpuCount:
                default: 0
                description: |-
                  GpuCount is the number of nvidia.com/gpu devices required by each pod.
                  Use 0 for CPU-only workloads or for local Kind demos without a device plugin.
                format: int32
                minimum: 0
                type: integer
              image:
                description: Image is the container image to run, e.g. nvcr.io/nvidia/tritonserver:24.05-py3.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels are merged into pod and deployment labels (metadata
                  labels win).
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector constrains pods to nodes with matching labels.
                type: object
              replicas:
                description: Replicas is the desired number of replicas. Defaults
                  to 1.
                format: int32
                minimum: 0
                type: integer
              runtimeClassName:
                description: RuntimeClassName is an optional RuntimeClass, usually
                  "nvidia" in GPU clusters.
                type: string
              servicePort:
                default: 8000
                description: ServicePort is the port the Service should listen on.
                  Defaults to 8000.
                format: int32
                minimum: 1
                type: integer
              tolerations:
                description: Tolerations are copied to the pod template. Useful for
                  GPU node taints.
                items:
                  description: |-
                    Toleration is a small subset of corev1.Toleration used to keep the API
                    dependency-light while still supporting common GPU node taints.
                  properties:
                    effect:
                      description: Effect indicates the taint effect to match.
                      type: string
                    key:
                      description: Key is the taint key the toleration applies to.
                      type: string
                    operator:
                      description: Operator represents a key's relationship to the
                        value.
                      type: string
                    value:
                      description: Value is the taint value the toleration matches
                        to.
                      type: string
                  type: object
                type: array
            required:
            - image
            type: object
          status:
            description: GpuWorkloadStatus defines the observed state of GpuWorkload.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available pod replicas
                  reported by Deployment.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deploymentName:
                description: DeploymentName is the name of the managed Deployment.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller.
                format: int64
                type: integer
              phase:
                description: 'Phase is a human-friendly summary: "", "Pending", "Ready",
                  or "Degraded".'
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready pod replicas.
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the name of the managed Service.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
